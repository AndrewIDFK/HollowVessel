using System;
using HollowVessel.UI;
using System.Collections.Generic;
using Microsoft.Xna.Framework;
using Terraria;
using Terraria.ID;
using Terraria.ModLoader;

namespace HollowVessel
{
	public class HollowGlobalItem : GlobalItem
	{			
		/*public virtual void OnHitNPC(Item item, NPC target, int damage, float knockback, bool crit)
		{
			if (item.owner == Main.myPlayer)
			{
				var modPlayer = Main.LocalPlayer.GetModPlayer<SoulMeterPlayer>();
				modPlayer.soulMeterCurrent += 11;
			}
		}*/
	}
}
